#write a python program to the contents of a directory using os 
#module.Search online for function which does that.

import os #importing the module
print(os.listdir()) # function to display all the directory content